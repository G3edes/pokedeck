# Pokémon TCG API key (optional).
# Without a key the API works with lower rate limits (~30 req/min shared).
# Get a free key at https://dev.pokemontcg.io/ to raise that limit for your own use.
VITE_POKEMON_TCG_API_KEY=
